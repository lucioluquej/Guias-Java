Algoritmo sin_titulo
	
//	Realizar una función que calcule y retorne la suma de todos los divisores del número n distin-
	//	tos de n. El valor de n debe ser ingresado por el usuario.
	
	definir n Como Entero
	escribir "ingrese un número"
	leer n
	escribir "la suma de todos los divisores de " n " es " divisores(n)	
	
FinAlgoritmo

Funcion divi <- divisores(n)
	definir divi, i como entero
	divi=0
	para i <- 1 hasta n-1
		si n mod i =0
			divi=divi+i
		FinSi		
	FinPara
	
FinFuncion
